// We make use of aux as an wrapping element
const aux = (props) => props.children;

// Auxiliary elements are something that does not have semantic purpose but 
// exist for the purpose of grouping elements, styling, etc

export default aux;